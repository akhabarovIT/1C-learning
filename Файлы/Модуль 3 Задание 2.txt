1. Error calling context method (ExecuteCommand):
Не открылась гиперссылка, так как в предыдущем шаге возникла ошибка при загрузке документа.

2. Expected number of lines in <BasisesTree> table is равно 0 and actual number is <2>.
В 5 строке заказов, я так понимаю, не должно было быть документов. Ожидалось 0 строк, а по итогу их там 2.

3. Form table named <List> is not found.
Не сработала кнопка "Post and close" из предыдущего шага, так как была ошибка в сообщениях.

4. Line No.
В столбце Branch отсутствуют названия "Shop 01", и почему-то тест воспринимает * как значение, а не любая переменная.

5. The button/command with <Sales invoice> header is not found.
Из-за ошибки предыдущего шага, которая осталась открытой, кнопка не была обнаружена.

6. Unable to move to the required line in <BasisesTree> table.
В обоих случаях не был осуществлен выбор указанных строк, так как внутри этих строк есть данные, которых нет в таблице.
В 1 варианте нет строки с количеством "7000", но есть "5000" с такими же остальными данными.
Во 2 варианте вообще нет такой строки, которую нужно выбрать.
